dist/
coverage/
# Build output: the embed bundle and the dashboard's bundle and stylesheet. All
# three are minified, and Prettier reformatting a bundle checks esbuild's output
# rather than anybody's source. `pnpm build` writes them into the working tree, so
# without this a local `pnpm check` after a build fails on generated files.
public/
.wrangler/
node_modules/
.claude/
pnpm-lock.yaml
src/worker-configuration.d.ts

# Prose is hand-wrapped at 80 columns and its tables are hand-aligned; Prettier
# reflows both, which turns every documentation edit into a diff of padding.
*.md
